var number1 = parseInt(prompt("Enter your weight:"));
var number2 = parseInt(prompt("Enter another number:"));

var division = function(number1, number2){
  return number1 / number2;
};

prompt(division(number1, number2));
